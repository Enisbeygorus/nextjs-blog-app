import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog App",
  description: "Find and discover blog to read decent contents.",
};

import Home from "../pages/Home";

export default Home;
